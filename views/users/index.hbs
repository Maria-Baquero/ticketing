<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">

</head>

<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">

            <li class="nav-item">
                <a class="nav-link active" href="/api/auth/dashboard">Dashboard</a>
            </li>

            <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                <li class="nav-item">
                    <a class="nav-link" href="#">My Tickets</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="#">Create Ticket</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="#">Users</a>
                </li>
            </ul>


            <div class="d-flex align-items-center">

                <span class="text-white me-3">
                    Welcome, User
                </span>

            </div>
        </div>
    </nav>


    
        <div class="container-fluid d-flex justify-content-end">
            <a href="/api/users/create" class="btn btn-primary me-3 mt-5">
                Create user
            </a>
        </div>
    



    <div class="container mt-5">

        <h3 class="mb-4">User list</h3>

        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    
                    <th>Name</th>
                    <th>Email</th>
                    <th>Telephone</th>
                    <th>DNI</th>
                    <th>Rol</th>
                    <th>Status</th>
                    <th>Google</th>
                    <th>Image</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {{#each users}}
                <tr>
                    
                    <td>{{name}}</td>
                    <td>{{email}}</td>
                    <td>{{telephone}}</td>
                    <td>{{dni}}</td>
                    <td>{{rol}}</td>
                    <td>{{#if status}}Active{{else}}Inactive{{/if}}</td>
                    <td>{{#if google}}Yes{{else}}No{{/if}}</td>
                    <td>
                        {{#if img}}
                        <img src="{{img}}" alt="Photo of {{name}}" width="50" height="50">
                        {{else}}
                        -
                        {{/if}}
                    </td>
                    <td>
                        <a href="/api/users/edit/{{_id}}" class="btn btn-primary">
                            Edit user
                        </a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>